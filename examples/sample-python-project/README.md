# notes-api-py

A tiny notes service. This fixture seeds Mantyl's Python verifier tests.

All requests are rate limited per client. Notes persist to durable
storage between restarts.

Run the tests with pytest after installing requirements.txt.
