# notes-api

A tiny notes API.

- `npm run build` then `node dist/server.js`
- All requests are rate limited. <!-- SEEDED QUIRK: false claim — no rate
  limiting exists in this codebase. Mantyl must mark it contradicted. -->
- Configure via `.env` (see `.env.example`).
