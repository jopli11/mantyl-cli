---
description: Run the Mantyl passport loop - doctor, ci, report findings honestly, offer publish and verified.
---

Run the passport skill's loop on this project now: check the
environment with `npx mantyl doctor`, make sure `.mantyl/` is
gitignored, run `npx mantyl ci`, then read `.mantyl/passport.md` and
report contradicted claims, failed checks, risks and undocumented
environment variables, in that order, without softening anything.
Offer `npx mantyl publish` and `npx mantyl verified` as the user's
choices, never run them unprompted. $ARGUMENTS
